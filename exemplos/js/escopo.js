'use strict';

var mensagem = '';

function enviar() {
  var nome = '...';

  if (nome === '') {
    var mensagem = 'Nome não digitado!';
    alert(mensagem);
  }

  function interna() {
    var variavel = '' ;
  }

  mensagem = '';
}
